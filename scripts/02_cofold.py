#!/usr/bin/env python3
"""Stage 2 - Chai-1 co-folding, 5 seeds, single-sequence mode

STATUS: STUBBED.

This script is a placeholder. It exits non-zero rather than emitting an empty
or fabricated output file, so that a stubbed stage can never be mistaken for a
stage that ran. Implement it, then remove this block and update the stage table
in README.md.
"""
import sys

STAGE = "02_cofold"

sys.exit(
    f"STUBBED: {STAGE} is not yet implemented. "
    "No output was written. See README.md for the stage status table."
)
